import NavButton from './NavButton.jsx';

function Page(props) {
  return (
    <div>
      <title>{props.page_title}</title>
      <h1>{props.page_title}</h1>
    </div>
  );
}

export default Page;
